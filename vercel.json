{
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "npm run build:client-app:prod",
  "outputDirectory": "client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
